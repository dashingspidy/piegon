<!DOCTYPE html>
<html class="scroll-smooth" lang="en">
  <head>
    <title>Piegon - <%= content_for(:title) || "Send marketing email with ease" %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name=”robots” content="index, follow">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="turbo-prefetch" content="false">
    
    <meta name="description" content="<%= content_for(:description) || "Pay once for lifetime email marketing with Piegon. Connect SMTP, launch campaigns, and track results — no monthly fees." %>">
    <meta name="keywords" content="<%= content_for(:keywords) || "email marketing platform, affordable email marketing, cost-effective email marketing, email marketing for businesses, unlimited email campaigns, mailchimp alternative" %>">
    
    <meta property="og:title" content="Piegon - <%= content_for(:title) || "Send marketing email with ease" %>">
    <meta property="og:description" content="<%= content_for(:description) || "Pay once for lifetime email marketing with Piegon. Connect SMTP, launch campaigns, and track results — no monthly fees." %>">
    <meta property="og:image" content="<%= content_for(:og_image) || asset_url('logo-email.png') %>">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:type" content="website">
    <meta property="og:logo" content="<%= asset_url('logo-email.png') %>">
    
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Piegon - <%= content_for(:title) || "Send marketing email with ease" %>">
    <meta name="twitter:description" content="<%= content_for(:description) || "Pay once for lifetime email marketing with Piegon. Connect SMTP, launch campaigns, and track results — no monthly fees." %>">
    <meta name="twitter:image" content="<%= content_for(:og_image) || asset_url('logo-email.png') %>">
    
    <link rel="canonical" href="<%= request.original_url %>">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <script defer src="https://cloud.umami.is/script.js" data-website-id="d29bb4a6-ab44-4799-ade8-fd35cdc32ef6"></script>
    <script>
        !function(t,e){var o,n,p,r;e.__SV||(window.posthog=e,e._i=[],e.init=function(i,s,a){function g(t,e){var o=e.split(".");2==o.length&&(t=t[o[0]],e=o[1]),t[e]=function(){t.push([e].concat(Array.prototype.slice.call(arguments,0)))}}(p=t.createElement("script")).type="text/javascript",p.crossOrigin="anonymous",p.async=!0,p.src=s.api_host+"/static/array.js",(r=t.getElementsByTagName("script")[0]).parentNode.insertBefore(p,r);var u=e;for(void 0!==a?u=e[a]=[]:a="posthog",u.people=u.people||[],u.toString=function(t){var e="posthog";return"posthog"!==a&&(e+="."+a),t||(e+=" (stub)"),e},u.people.toString=function(){return u.toString(1)+".people (stub)"},o="capture identify alias people.set people.set_once set_config register register_once unregister opt_out_capturing has_opted_out_capturing opt_in_capturing reset isFeatureEnabled onFeatureFlags getFeatureFlag getFeatureFlagPayload reloadFeatureFlags group updateEarlyAccessFeatureEnrollment getEarlyAccessFeatures getActiveMatchingSurveys getSurveys getNextSurveyStep".split(" "),n=0;n<o.length;n++)g(u,o[n]);e._i.push([i,s,a])},e.__SV=1)}(document,window.posthog||[]);
        posthog.init('phc_fgtQKm5pwRhdtVGTTRu2OOfphMtDVrnO1jS6mB3omy7',{api_host:'https://eu.i.posthog.com'})
    </script>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= Sentry.get_trace_propagation_meta.html_safe %>
  </head>

  <body>
    <% if authenticated? %>
        <%= render 'layouts/sidenav' %>
        <%= render "layouts/flash" %>
    <% else %>
      <%= render "layouts/nav" %>
      <main>
        <%= render "layouts/flash" %>
        <%= yield %>
      </main>
      <%= render "layouts/footer" %>
    <% end %>
    
    <% sentry_id = request.env["sentry.error_event_id"] %>
      <% if sentry_id.present? %>
      <script>
        // Wait for Sentry to be available and show error dialog
        if (window.Sentry && typeof window.Sentry.showReportDialog === 'function') {
          window.Sentry.showReportDialog({ eventId: "<%= sentry_id %>" });
        } else {
          // Retry after a short delay if Sentry isn't loaded yet
          setTimeout(function() {
            if (window.Sentry && typeof window.Sentry.showReportDialog === 'function') {
              window.Sentry.showReportDialog({ eventId: "<%= sentry_id %>" });
            }
          }, 100);
        }
      </script>
    <% end %>
  </body>
</html>
